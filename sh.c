// operators: < > << >> && || { } ! ; &

main(argc: int, argv: **byte, envp: **byte) {
	var a: alloc;
	setup_alloc(&a);
	// Parse args
	// Tokenize command
	// Parse command
	// Evaluate redirections
	// Execute command
}
